import * as React from 'react'
import * as S from './Footer.styles'

export const Footer: React.FC = (props) => {
  return (
    <S.Container {...props}>
      <code>.</code>
    </S.Container>
  )
}
